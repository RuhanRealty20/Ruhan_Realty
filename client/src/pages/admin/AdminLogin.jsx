import { useState } from 'react';
import { useLocation, useNavigate } from 'react-router-dom';
import { SEO } from '../../components/SEO';
import { useToast } from '../../context/ToastContext';
import { api } from '../../services/api';

export default function AdminLogin(){const [form,setForm]=useState({email:'',password:''});const [loading,setLoading]=useState(false);const {notify}=useToast();const navigate=useNavigate();const location=useLocation();const submit=async(e)=>{e.preventDefault();setLoading(true);try{const result=await api.auth.login(form);sessionStorage.setItem('rr_access_token',result.accessToken);navigate(location.state?.from?.pathname||'/admin',{replace:true})}catch(err){notify(err.message)}finally{setLoading(false)}};return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:'1rem',background:'linear-gradient(130deg,#081722,#244b59)'}}><SEO title="Admin login" description="Secure administration for RuhanRealty.com." noindex/><form className="card" onSubmit={submit} style={{width:'min(430px,100%)',padding:'2rem'}}><div className="brand"><strong>RUHAN SYED</strong><span>Lead command center</span></div><h1 style={{fontFamily:'var(--font-display)',fontSize:'2.5rem',margin:'2rem 0 1rem'}}>Welcome back.</h1><div className="field"><label htmlFor="email">Email</label><input id="email" type="email" autoComplete="username" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></div><div className="field" style={{marginTop:'1rem'}}><label htmlFor="password">Password</label><input id="password" type="password" autoComplete="current-password" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></div><button className="btn btn-primary" style={{width:'100%',marginTop:'1.5rem'}} disabled={loading}>{loading?'Signing in…':'Sign in securely'}</button><p className="consent">Protected by rate limiting, Argon2 password hashing, short-lived access tokens and rotating refresh sessions.</p></form></main>}
