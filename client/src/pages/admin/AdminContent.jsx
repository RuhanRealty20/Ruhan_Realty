import { useState } from 'react';

const collections=['Areas','Market insights','News & insights','Testimonials','FAQs','Lead magnets','Site content'];
export default function AdminContent(){const [active,setActive]=useState('Areas');return <><div className="admin-top"><div><p className="eyebrow">CMS</p><h1 style={{fontFamily:'var(--font-display)',fontSize:'2.5rem'}}>Content management</h1></div></div><div className="filters">{collections.map(c=><button className={`choice ${active===c?'selected':''}`} style={{padding:'.55rem .8rem'}} onClick={()=>setActive(c)} key={c}>{c}</button>)}</div><section className="card" style={{padding:'1.5rem'}}><h2>{active}</h2><p className="muted">The API content model supports drafts, review status and publishing controls. Public content requires human and compliance review; testimonials must be authentic and authorized.</p><div className="placeholder-panel">Connect this workspace to the content endpoints once MongoDB Atlas is configured. Seed scripts create development labels only, never fake public claims.</div></section></>}
