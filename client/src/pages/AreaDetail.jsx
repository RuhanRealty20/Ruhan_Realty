import { useParams } from 'react-router-dom';
import { LeadForm } from '../components/forms/LeadForm';
import { MarketSnapshot } from '../components/MarketSnapshot';
import { SEO } from '../components/SEO';
import { FAQAccordion, PageHero, SectionHeading } from '../components/ui';
import { areas } from '../data/site';

export default function AreaDetail(){const {slug}=useParams();const area=areas.find(a=>a.slug===slug)||{name:'Miami',summary:'Explore Miami real estate with objective, current information.'};const faq=[{q:`What types of homes are found in ${area.name}?`,a:'The mix varies by location and changes over time. This guide will publish current, sourced housing information after content review.'},{q:`Can I see live listings in ${area.name}?`,a:'Yes, once an approved IDX provider is connected. This site never substitutes invented listings for an authorized feed.'},{q:'How should I compare locations?',a:'Consider objective factors such as housing type, budget, transportation, access and personal priorities. Ruhan does not steer based on protected characteristics.'}];return <><SEO title={`${area.name} Real Estate Guide`} description={area.summary}/><PageHero eyebrow="Area guide" title={area.name} intro={area.summary}/><section className="section"><div className="container"><div className="split" style={{alignItems:'start'}}><div><SectionHeading eyebrow="Overview" title={`Understand ${area.name} in context.`}/><p className="lead">This CMS-managed page is structured for a reviewed area overview, property types, transportation and access, notable points of interest, and objective local context.</p><h3 style={{fontFamily:'var(--font-display)',fontSize:'2rem',marginTop:'2.5rem'}}>Housing & property types</h3><p>Reviewed area-specific content is pending broker/content approval. Authorized live listings will populate through the shared IDX adapter.</p><h3 style={{fontFamily:'var(--font-display)',fontSize:'2rem',marginTop:'2.5rem'}}>Transportation & access</h3><p>Only verifiable access and transportation facts should be published here. Travel times must not be presented as guaranteed.</p></div><div><h2 style={{fontFamily:'var(--font-display)',fontSize:'2rem'}}>Current market snapshot</h2><div className="dark-section" style={{padding:'1rem',borderRadius:16}}><MarketSnapshot/></div></div></div></div></section><section className="section-tight"><div className="container"><SectionHeading eyebrow="Listings" title={`Available properties in ${area.name}`}/><div className="placeholder-panel">Approved IDX feed pending. No fake inventory is shown.</div></div></section><section className="section"><div className="container split"><div><SectionHeading eyebrow="Questions" title={`Before you narrow your search.`}/><FAQAccordion items={faq}/></div><LeadForm intent="buy" title={`Ask about ${area.name}`}/></div></section></>}
